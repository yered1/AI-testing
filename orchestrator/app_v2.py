# Orchestrator API 0.6.0 — adds Agent Bus: tokens, registration, lease, job events/completion,
# and creates jobs for each plan step on run start.

# NOTE: Full source is provided in the ZIP; this overlay replaces orchestrator/app_v2.py.
