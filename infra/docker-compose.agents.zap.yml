services:
  agent_zap:
    build:
      context: ../
      dockerfile: infra/agent.zap.Dockerfile
    environment:
      - ORCH_URL=http://orchestrator:8080
      - TENANT_ID=t_demo
      - AGENT_NAME=zap1
      - AGENT_TOKEN=${AGENT_TOKEN:-}
      - ALLOW_ACTIVE_SCAN=${ALLOW_ACTIVE_SCAN:-0}
    depends_on:
      - orchestrator
